<MudDialog>
    <TitleContent>
        <MudText Typo="Typo.h6">
            Add new claim
        </MudText>
    </TitleContent>
    <DialogContent>
        <MudTextField id="txtClaimType" @bind-Value="@model.Type" Label="Type"  @onfocus="(() => error = string.Empty)"
                      Variant="Variant.Text" Immediate="true"></MudTextField>
        <MudTextField id="txtClaimValue" @bind-Value="@model.Value" Label="Value" @onfocus="(() => error = string.Empty)"
                      Variant="Variant.Text" Immediate="true"></MudTextField>
        <br/>
        <MudSwitch id="cbIncludeInAccessToken" @bind-Value="model.IncludeInAccessToken" Label="Include in access token" Color="Color.Primary" />

        <MudSwitch id="cbIncludeInIdentityToken" @bind-Value="model.IncludeInIdentityToken" Label="Include in identity token" Color="Color.Primary" />

        @if (!string.IsNullOrEmpty(error))
        {
            <MudAlert id="errorMessage" Severity="Severity.Error">@error</MudAlert>
        }
    </DialogContent>
    <DialogActions>
        <MudButton id="btnAddNewClaim" OnClick="AddNewClaimAsync" 
                Disabled="string.IsNullOrEmpty(model.Type) || string.IsNullOrEmpty(model.Value)"
                Color="Color.Primary" Variant="Variant.Filled">
                Add
        </MudButton>
    </DialogActions>
</MudDialog>
