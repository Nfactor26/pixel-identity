@page "/roles/edit/{name?}"
@attribute [Authorize(Policy = Policies.CanManageRoles)]

@if (model != null)
{
    <MudText Typo="Typo.h4">Edit Role</MudText>
    <br />

    <MudCard Elevation="4">
        <MudCardContent>
            <MudTextField Label="Role Name" Class="mt-3" HelperText="Required" Disabled="model.Exists"
                      @bind-Value="model.RoleName" For="@(() => model.RoleName)" />
        </MudCardContent>

    </MudCard>

    <br />
    <ClaimsManager Claims="model.Claims" OnAddItem="AddClaimAsync"
               OnUpdateItem="UpdateClaimAsync" OnDeleteItem="RemoveClaimAsync" />
}